/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "source.unsplash.com",
      "techcrunch.com",
      "github.com",
      "api.dicebear.com",
    ],
  },
};

export default nextConfig;
